{
  "name": "vhc",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "api:dev": "npm run dev --workspace=@vhc/api",
    "web:dev": "npm run dev --workspace=@vhc/web",
    "mobile:dev": "npm run dev --workspace=@vhc/mobile"
  },
  "devDependencies": {
    "serve": "^14.2.5",
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
